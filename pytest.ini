[pytest]
testpaths = tests 
addopts = -v --ctrf report.json
markers = 
    smoke: quick, critical tests — run before every commit
    regression: full test suite — run before release
    ui: browser-based tests
    api: API-based tests