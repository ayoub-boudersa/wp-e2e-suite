{
    "type": "object",
    "required": ["id", "name", "slug", "count"],
    "properties": {
        "id": {"type": "integer"},
        "name": {"type": "string"},
        "slug": {"type": "string"},
        "count": {"type": "integer"},
        "description": {"type": "string"}
    }
}