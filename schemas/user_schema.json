{
    "type": "object",
    "required": ["id", "name", "slug", "link"],
    "properties": {
        "id": {"type": "integer"},
        "name": {"type": "string"},
        "slug": {"type": "string"},
        "link": {"type": "string"}
    }
}